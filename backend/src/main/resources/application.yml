spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 897927429354-esjm7u3huu44uf8jgoe1vnfdi5n0ds0u.apps.googleusercontent.com
            client-secret: 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
            scope:
              - profile
              - email
  datasource:
    url: jdbc:mariadb://localhost:3307/habitflow
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
jwt:
  secret: habitflowsecretkeywhichisverylongandsecureenoughforhs256algorithm2026
file:
  upload:
    dir: ./uploads